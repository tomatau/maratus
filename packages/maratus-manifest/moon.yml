tasks:
  build:
    deps:
      - 'tools-build-registry:build'
    command: 'bun'
    args:
      - 'run'
      - 'build'
    inputs:
      - '@group(repo-config)'
      - '@group(registry-meta)'
      - '@group(codemod-meta)'
