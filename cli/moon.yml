dependsOn:
  - 'codemod-runner'

tasks:
  build:
    extends: 'platform-cli-path'
    command: 'go'
    args:
      - 'build'
      - '-o'
      - '@out(0)'
      - '-ldflags=-s -w'
      - '.'

  test-integration:
    command: 'go'
    args:
      - 'test'
      - './...'
