# Changesets

Changesets records release intent for Maratus packages.

Use `bun run changeset` to create a changeset when a pull request changes the
behaviour, API, or publishable output of releasable packages.
