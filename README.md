# arachne

## Commands

- Install deps:
  - `bun install`
- Run tests (workspace):
  - `bun test`
- Build CLI binary:
  - `bun --cwd packages/cli run build`
- Run compiled CLI:
  - `./packages/cli/dist/arachne`
