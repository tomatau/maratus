{
  "name": "@maratus-codemod/{{ name | kebab_case}}",
  "version": "0.1.0",
  "private": false,
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": "./dist/index.mjs"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --target=node --outfile ./dist/index.mjs",
    "test:unit": "bun test"
  },
  "dependencies": {
    "@maratus/codemod-runner": "{{ runner_version }}"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
